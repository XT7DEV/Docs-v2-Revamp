---
title: boolean
description:
icon: polytoria/Bool
---

# :polytoria-Bool: boolean

A `boolean` or `bool` is a variable that can be either `true` or `false`.

Booleans come from the way computers interpret data with `1` being `true` and `0` being `false`.

Think of a light switch: `True` would be when the light is `on` or `1`. `False` would be when the light is `off` or `0`.

[Read More (Lua 5.1 Docs)](https://www.lua.org/manual/5.1/manual.html#2.2)